/*var TableData = require('../models/tableData').TableData;

module.exports = function(req, res, next) {
    TableData.find(function (err, tableData) {
        if (err) return console.error(err);
        console.log(tableData);
    });
    next();
};
*/